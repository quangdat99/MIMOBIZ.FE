import Command from './constants/Command';
import Compare from './constants/Compare';
import FormState from './constants/FormState';
import Gender from './constants/Gender';
import ModelState from './constants/ModelState';
import Role from './constants/Role';
import SaveMode from './constants/SaveMode';
import ValidateResultType from './constants/ValidateResultType';

export const MSConstant = {
  Command,
  Compare,
  FormState,
  Gender,
  ModelState,
  Role,
  SaveMode,
  ValidateResultType,
};

export default MSConstant;